language: python
python:
  - "3.6"
cache: pip
install:
  - pip install -r requirements.txt
  - pip install tox-travis coveralls
  - pip install codecov
script:
  - tox
  - coverage run --source ctci -m py.test
after_success:
  - codecov
